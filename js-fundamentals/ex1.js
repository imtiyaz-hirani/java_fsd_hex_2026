// JS Variables 

// In JS, unlike Java, we do not specify the data types of variables 

let name="John"  // string 
const age=23   //const is like a final, age value cannot change now --number
var city = 'mumbai' //This is an old way , so avoid this 

console.log(name + " " + age + " " + city); 

let active = true // This is a boolean 
console.log(typeof active)

let val = null // intentionally giving null 
console.log(typeof val)

let state; // This is undefined 
let state1 = undefined // This is also undefined 

console.log(typeof state) 
console.log(typeof state1)

/**
 * string ''
 * number 11 , 1.3, 5656
 * boolean
 * undefined
 */